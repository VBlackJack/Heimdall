<Window x:Class="Heimdall.App.Views.Dialogs.MessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title=""
        Width="460" SizeToContent="Height"
        MinWidth="350" MinHeight="160" MaxHeight="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Icon + Title -->
        <!-- DockPanel, not a horizontal StackPanel: the latter measures its children with
             infinite width, so TitleText's TextWrapping never fires and a long title is clipped
             at the window edge instead of wrapping. The window is Width="460" with
             SizeToContent="Height", so the width never grows to rescue it. Titles are
             caller-supplied and unbounded - EmbeddedSftpViewModel passes a remote file name into
             SftpPropertiesTitle. Same defect a user reported on the connection-test chip, where
             it silently ate eight characters out of the middle of a sentence. -->
        <DockPanel Grid.Row="0" Margin="0,0,0,12" LastChildFill="True">
            <TextBlock x:Name="IconText"
                       DockPanel.Dock="Left"
                       FontFamily="Segoe MDL2 Assets"
                       FontSize="{StaticResource FontSizeHeadline}"
                       VerticalAlignment="Center"
                       Margin="0,0,12,0"/>
            <TextBlock x:Name="TitleText"
                       FontSize="{StaticResource FontSizeSubtitle}"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap"/>
        </DockPanel>

        <!-- Message -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="MessageText"
                       FontSize="{StaticResource FontSizeBodyLarge}"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,16"/>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="BtnTertiary"
                    Click="OnTertiaryClick"
                    AutomationProperties.Name="{Binding Content, RelativeSource={RelativeSource Self}}"
                    Style="{DynamicResource SecondaryButtonStyle}"
                    MinWidth="80" Padding="16,6" FontSize="{StaticResource FontSizeBody}"
                    Visibility="Collapsed"
                    IsCancel="True"
                    Margin="0,0,8,0"/>
            <Button x:Name="BtnSecondary"
                    Click="OnSecondaryClick"
                    AutomationProperties.Name="{Binding Content, RelativeSource={RelativeSource Self}}"
                    Style="{DynamicResource SecondaryButtonStyle}"
                    MinWidth="80" Padding="16,6" FontSize="{StaticResource FontSizeBody}"
                    Visibility="Collapsed"
                    Margin="0,0,8,0"/>
            <Button x:Name="BtnPrimary"
                    Click="OnPrimaryClick"
                    AutomationProperties.Name="{Binding Content, RelativeSource={RelativeSource Self}}"
                    Style="{DynamicResource PrimaryButtonStyle}"
                    MinWidth="80" Padding="16,6" FontSize="{StaticResource FontSizeBody}"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
