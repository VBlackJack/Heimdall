<Window x:Class="Heimdall.App.Views.Dialogs.CommandActionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:Heimdall.App.Converters"
        xmlns:loc="clr-namespace:Heimdall.App.Localization"
        xmlns:vm="clr-namespace:Heimdall.App.ViewModels.Dialogs"
        xmlns:enums="clr-namespace:TwinShell.Core.Enums;assembly=TwinShell.Core"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:CommandActionDialogViewModel}"
        Title="{Binding DialogTitle}"
        Width="620" SizeToContent="Height" MaxHeight="780"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        ResizeMode="CanResizeWithGrip"
        MinWidth="500" MinHeight="400"
        ShowInTaskbar="False"
        Closing="OnWindowClosing">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/DialogCommonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:NullToVisibilityConverter x:Key="NullToVis"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="{StaticResource SpacingLg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ═══════════ FORM FIELDS (scrollable) ═══════════ -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Title -->
                <TextBlock x:Name="LblTitle" Text="{loc:Translate ToolCmdLibDialogLblTitle}" Style="{StaticResource DialogLabelStyle}"/>
                <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                         AutomationProperties.Name="{loc:Translate ToolCmdLibDialogLblTitle}"
                         Style="{StaticResource DialogFormTextBoxStyle}"
                         MaxLength="200" x:Name="TxtTitle" TabIndex="0"/>
                <TextBlock Text="{Binding TitleError}"
                           Style="{StaticResource FieldValidationErrorStyle}"
                           Visibility="{Binding TitleError, Converter={StaticResource NullToVis}}"/>

                <!-- Category (editable ComboBox) -->
                <TextBlock x:Name="LblCategory" Text="{loc:Translate ToolCmdLibDialogLblCategory}" Style="{StaticResource DialogLabelStyle}" Margin="0,12,0,4"/>
                <ComboBox ItemsSource="{Binding AvailableCategories}"
                          AutomationProperties.Name="{loc:Translate ToolCmdLibDialogLblCategory}"
                          Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}"
                          IsEditable="True"
                          Style="{StaticResource DialogFormComboBoxStyle}"
                          x:Name="CmbCategory" TabIndex="1"/>
                <TextBlock Text="{Binding CategoryError}"
                           Style="{StaticResource FieldValidationErrorStyle}"
                           Visibility="{Binding CategoryError, Converter={StaticResource NullToVis}}"/>

                <!-- Platform + Risk (horizontal) -->
                <Grid Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="LblPlatform" Text="{loc:Translate ToolCmdLibDialogLblPlatform}" Style="{StaticResource DialogLabelStyle}"/>
                        <ComboBox x:Name="CmbPlatform"
                                  AutomationProperties.Name="{loc:Translate ToolCmdLibDialogLblPlatform}"
                                  SelectedIndex="{Binding Platform}"
                                  Style="{StaticResource DialogFormComboBoxStyle}" TabIndex="2">
                            <ComboBoxItem x:Name="PlatformWindows" Content="{loc:Translate ToolCmdLibPlatformWindows}"/>
                            <ComboBoxItem x:Name="PlatformLinux" Content="{loc:Translate ToolCmdLibPlatformLinux}"/>
                            <ComboBoxItem x:Name="PlatformBoth" Content="{loc:Translate ToolCmdLibPlatformBoth}"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock x:Name="LblRisk" Text="{loc:Translate ToolCmdLibDialogLblRisk}" Style="{StaticResource DialogLabelStyle}"/>
                        <ComboBox x:Name="CmbRisk"
                                  AutomationProperties.Name="{loc:Translate ToolCmdLibDialogLblRisk}"
                                  SelectedIndex="{Binding Level}"
                                  Style="{StaticResource DialogFormComboBoxStyle}" TabIndex="3">
                            <ComboBoxItem x:Name="RiskInfo" Content="{loc:Translate ToolCmdLibRiskInfo}"/>
                            <ComboBoxItem x:Name="RiskRun" Content="{loc:Translate ToolCmdLibRiskRun}"/>
                            <ComboBoxItem x:Name="RiskDangerous" Content="{loc:Translate ToolCmdLibRiskDangerous}"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <!-- Description -->
                <TextBlock x:Name="LblDescription" Text="{loc:Translate ToolCmdLibDialogLblDescription}" Style="{StaticResource DialogLabelStyle}" Margin="0,12,0,4"/>
                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                         AutomationProperties.Name="{Binding Text, ElementName=LblDescription}"
                         Style="{StaticResource DialogFormTextBoxStyle}"
                         TextWrapping="Wrap" AcceptsReturn="True"
                         Height="60" VerticalContentAlignment="Top"
                         VerticalScrollBarVisibility="Auto"
                         MaxLength="2000" TabIndex="4"/>

                <!-- ── Windows Template ────────────────────── -->
                <StackPanel x:Name="WindowsSection"
                            Visibility="{Binding ShowWindowsSection, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock x:Name="LblWindowsSection"
                           Text="{loc:Translate ToolCmdLibDialogSectionWindows}"
                           Style="{StaticResource DialogSectionTitleStyle}"
                           Margin="0,16,0,4"/>

                <TextBlock x:Name="LblWinPattern" Text="{loc:Translate ToolCmdLibDialogLblPattern}" Style="{StaticResource DialogLabelStyle}"/>
                <TextBox Text="{Binding WindowsPattern, UpdateSourceTrigger=PropertyChanged}"
                         AutomationProperties.Name="{Binding Text, ElementName=LblWinPattern}"
                         Style="{StaticResource DialogFormTextBoxStyle}"
                         FontFamily="Consolas" TabIndex="5"/>

                <TextBlock x:Name="LblWinName" Text="{loc:Translate ToolCmdLibDialogLblTemplateName}" Style="{StaticResource DialogLabelStyle}" Margin="0,4,0,4"/>
                <TextBox Text="{Binding WindowsTemplateName, UpdateSourceTrigger=PropertyChanged}"
                         AutomationProperties.Name="{Binding Text, ElementName=LblWinName}"
                         Style="{StaticResource DialogFormTextBoxStyle}" TabIndex="6"/>

                <!-- Windows parameters -->
                <Grid Margin="0,8,0,2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80"/><ColumnDefinition Width="24"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" x:Name="LblWinParamName" Text="{loc:Translate ToolCmdLibDialogParamName}" FontSize="{StaticResource FontSizeSmallCaption}" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" x:Name="LblWinParamLabel" Text="{loc:Translate ToolCmdLibDialogParamLabel}" FontSize="{StaticResource FontSizeSmallCaption}" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" x:Name="LblWinParamType" Text="{loc:Translate ToolCmdLibDialogParamType}" FontSize="{StaticResource FontSizeSmallCaption}" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="SemiBold"/>
                </Grid>
                <ItemsControl ItemsSource="{Binding WindowsParameters}" Margin="0,0,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,2,0,4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="24"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                             AutomationProperties.Name="{loc:Translate ToolCmdLibDialogParamName}"
                                             FontFamily="Consolas" Margin="0,0,4,0" Padding="4,3"
                                             FontSize="{StaticResource FontSizeCaption}"
                                             Background="{DynamicResource CardBrush}"
                                             Foreground="{DynamicResource TextPrimaryBrush}"
                                             BorderBrush="{DynamicResource BorderBrush}"/>
                                    <TextBox Grid.Column="1" Text="{Binding Label, UpdateSourceTrigger=PropertyChanged}"
                                             AutomationProperties.Name="{loc:Translate ToolCmdLibDialogParamLabel}"
                                             Margin="0,0,4,0" Padding="4,3"
                                             FontSize="{StaticResource FontSizeCaption}"
                                             Background="{DynamicResource CardBrush}"
                                             Foreground="{DynamicResource TextPrimaryBrush}"
                                             BorderBrush="{DynamicResource BorderBrush}"/>
                                    <ComboBox Grid.Column="2" ItemsSource="{x:Static vm:ParameterEntryVm.AvailableTypes}"
                                              AutomationProperties.Name="{loc:Translate ToolCmdLibDialogParamType}"
                                              SelectedValue="{Binding Type}" Margin="0,0,4,0"
                                              FontSize="{StaticResource FontSizeCaption}"/>
                                    <Button Grid.Column="3" Content="&#xE711;" FontFamily="Segoe MDL2 Assets"
                                            FontSize="10" Width="20" Height="20"
                                        AutomationProperties.Name="{loc:Translate ToolCmdLibBtnDelete}"
                                        Command="{Binding DataContext.RemoveWindowsParameterCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        ToolTip="{loc:Translate ToolCmdLibBtnDelete}"
                                        Style="{DynamicResource GhostButtonStyle}"/>
                                </Grid>
                                <Grid Margin="0,2,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" Text="{Binding DefaultValue, UpdateSourceTrigger=PropertyChanged}"
                                             AutomationProperties.Name="{loc:Translate ToolCmdLibDialogParamDefault}"
                                             Style="{StaticResource WatermarkTextBoxStyle}"
                                             Margin="0,0,4,0" Padding="4,3"
                                             Tag="{loc:Translate ToolCmdLibDialogParamDefault}"
                                             FontSize="{StaticResource FontSizeSmallCaption}"
                                             Background="{DynamicResource CardBrush}"
                                             Foreground="{DynamicResource TextSecondaryBrush}"
                                             BorderBrush="{DynamicResource BorderBrush}"/>
                                    <CheckBox Grid.Column="1" IsChecked="{Binding Required}"
                                              AutomationProperties.Name="{loc:Translate ToolCmdLibDialogParamRequired}"
                                              Content="{loc:Translate ToolCmdLibDialogParamRequired}"
                                              FontSize="{StaticResource FontSizeSmallCaption}"
                                              Foreground="{DynamicResource TextSecondaryBrush}"
                                              VerticalAlignment="Center"/>
                                </Grid>
                                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource WatermarkTextBoxStyle}"
                                         Tag="{loc:Translate ToolCmdLibDialogParamDescription}"
                                         AutomationProperties.Name="{loc:Translate ToolCmdLibDialogParamDescription}"
                                         Margin="0,2,4,0" Padding="4,3"
                                         FontSize="{StaticResource FontSizeSmallCaption}"
                                         Background="{DynamicResource CardBrush}"
                                         Foreground="{DynamicResource TextSecondaryBrush}"
                                         BorderBrush="{DynamicResource BorderBrush}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button x:Name="BtnAddWinParam" Content="{loc:Translate ToolCmdLibDialogBtnAddParam}"
                        Command="{Binding AddWindowsParameterCommand}"
                        AutomationProperties.Name="{Binding Content, RelativeSource={RelativeSource Self}}"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        FontSize="{StaticResource FontSizeCaption}"
                        Padding="8,2" HorizontalAlignment="Left" Margin="0,4,0,0" TabIndex="7"/>
                </StackPanel>

                <!-- ── Linux Template ──────────────────────── -->
                <StackPanel x:Name="LinuxSection"
                            Visibility="{Binding ShowLinuxSection, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock x:Name="LblLinuxSection"
                           Text="{loc:Translate ToolCmdLibDialogSectionLinux}"
                           Style="{StaticResource DialogSectionTitleStyle}"
                           Margin="0,16,0,4"/>

                <TextBlock x:Name="LblLinuxPattern" Text="{loc:Translate ToolCmdLibDialogLblPattern}" Style="{StaticResource DialogLabelStyle}"/>
                <TextBox Text="{Binding LinuxPattern, UpdateSourceTrigger=PropertyChanged}"
                         AutomationProperties.Name="{Binding Text, ElementName=LblLinuxPattern}"
                         Style="{StaticResource DialogFormTextBoxStyle}"
                         FontFamily="Consolas" TabIndex="8"/>

                <TextBlock x:Name="LblLinuxName" Text="{loc:Translate ToolCmdLibDialogLblTemplateName}" Style="{StaticResource DialogLabelStyle}" Margin="0,4,0,4"/>
                <TextBox Text="{Binding LinuxTemplateName, UpdateSourceTrigger=PropertyChanged}"
                         AutomationProperties.Name="{Binding Text, ElementName=LblLinuxName}"
                         Style="{StaticResource DialogFormTextBoxStyle}" TabIndex="9"/>

                <!-- Linux parameters -->
                <Grid Margin="0,8,0,2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80"/><ColumnDefinition Width="24"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" x:Name="LblLinuxParamName" Text="{loc:Translate ToolCmdLibDialogParamName}" FontSize="{StaticResource FontSizeSmallCaption}" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" x:Name="LblLinuxParamLabel" Text="{loc:Translate ToolCmdLibDialogParamLabel}" FontSize="{StaticResource FontSizeSmallCaption}" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" x:Name="LblLinuxParamType" Text="{loc:Translate ToolCmdLibDialogParamType}" FontSize="{StaticResource FontSizeSmallCaption}" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="SemiBold"/>
                </Grid>
                <ItemsControl ItemsSource="{Binding LinuxParameters}" Margin="0,4,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,2,0,4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="24"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                             AutomationProperties.Name="{loc:Translate ToolCmdLibDialogParamName}"
                                             FontFamily="Consolas" Margin="0,0,4,0" Padding="4,3"
                                             FontSize="{StaticResource FontSizeCaption}"
                                             Background="{DynamicResource CardBrush}"
                                             Foreground="{DynamicResource TextPrimaryBrush}"
                                             BorderBrush="{DynamicResource BorderBrush}"/>
                                    <TextBox Grid.Column="1" Text="{Binding Label, UpdateSourceTrigger=PropertyChanged}"
                                             AutomationProperties.Name="{loc:Translate ToolCmdLibDialogParamLabel}"
                                             Margin="0,0,4,0" Padding="4,3"
                                             FontSize="{StaticResource FontSizeCaption}"
                                             Background="{DynamicResource CardBrush}"
                                             Foreground="{DynamicResource TextPrimaryBrush}"
                                             BorderBrush="{DynamicResource BorderBrush}"/>
                                    <ComboBox Grid.Column="2" ItemsSource="{x:Static vm:ParameterEntryVm.AvailableTypes}"
                                              AutomationProperties.Name="{loc:Translate ToolCmdLibDialogParamType}"
                                              SelectedValue="{Binding Type}" Margin="0,0,4,0"
                                              FontSize="{StaticResource FontSizeCaption}"/>
                                    <Button Grid.Column="3" Content="&#xE711;" FontFamily="Segoe MDL2 Assets"
                                            FontSize="10" Width="20" Height="20"
                                        AutomationProperties.Name="{loc:Translate ToolCmdLibBtnDelete}"
                                        Command="{Binding DataContext.RemoveLinuxParameterCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        ToolTip="{loc:Translate ToolCmdLibBtnDelete}"
                                        Style="{DynamicResource GhostButtonStyle}"/>
                                </Grid>
                                <Grid Margin="0,2,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" Text="{Binding DefaultValue, UpdateSourceTrigger=PropertyChanged}"
                                             AutomationProperties.Name="{loc:Translate ToolCmdLibDialogParamDefault}"
                                             Style="{StaticResource WatermarkTextBoxStyle}"
                                             Margin="0,0,4,0" Padding="4,3"
                                             Tag="{loc:Translate ToolCmdLibDialogParamDefault}"
                                             FontSize="{StaticResource FontSizeSmallCaption}"
                                             Background="{DynamicResource CardBrush}"
                                             Foreground="{DynamicResource TextSecondaryBrush}"
                                             BorderBrush="{DynamicResource BorderBrush}"/>
                                    <CheckBox Grid.Column="1" IsChecked="{Binding Required}"
                                              AutomationProperties.Name="{loc:Translate ToolCmdLibDialogParamRequired}"
                                              Content="{loc:Translate ToolCmdLibDialogParamRequired}"
                                              FontSize="{StaticResource FontSizeSmallCaption}"
                                              Foreground="{DynamicResource TextSecondaryBrush}"
                                              VerticalAlignment="Center"/>
                                </Grid>
                                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource WatermarkTextBoxStyle}"
                                         Tag="{loc:Translate ToolCmdLibDialogParamDescription}"
                                         AutomationProperties.Name="{loc:Translate ToolCmdLibDialogParamDescription}"
                                         Margin="0,2,4,0" Padding="4,3"
                                         FontSize="{StaticResource FontSizeSmallCaption}"
                                         Background="{DynamicResource CardBrush}"
                                         Foreground="{DynamicResource TextSecondaryBrush}"
                                         BorderBrush="{DynamicResource BorderBrush}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button x:Name="BtnAddLinuxParam" Content="{loc:Translate ToolCmdLibDialogBtnAddParam}"
                        Command="{Binding AddLinuxParameterCommand}"
                        AutomationProperties.Name="{Binding Content, RelativeSource={RelativeSource Self}}"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        FontSize="{StaticResource FontSizeCaption}"
                        Padding="8,2" HorizontalAlignment="Left" Margin="0,4,0,0" TabIndex="10"/>

                </StackPanel>

                <!-- Tags -->
                <TextBlock x:Name="LblTags" Text="{loc:Translate ToolCmdLibDialogLblTags}" Style="{StaticResource DialogLabelStyle}" Margin="0,12,0,4"/>
                <TextBox Text="{Binding Tags, UpdateSourceTrigger=PropertyChanged}"
                         AutomationProperties.Name="{Binding Text, ElementName=LblTags}"
                         Style="{StaticResource DialogFormTextBoxStyle}" TabIndex="11"/>

                <!-- Notes -->
                <TextBlock x:Name="LblNotes" Text="{loc:Translate ToolCmdLibDialogLblNotes}" Style="{StaticResource DialogLabelStyle}" Margin="0,12,0,4"/>
                <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                         AutomationProperties.Name="{Binding Text, ElementName=LblNotes}"
                         Style="{StaticResource DialogFormTextBoxStyle}"
                         TextWrapping="Wrap" AcceptsReturn="True"
                         Height="50" VerticalContentAlignment="Top"
                         VerticalScrollBarVisibility="Auto"
                         MaxLength="5000" TabIndex="12"/>

                <!-- ── Examples ────────────────────────────── -->
                <TextBlock Style="{StaticResource DialogSectionTitleStyle}"
                           Text="{loc:Translate ToolCmdLibDialogSectionExamples}"
                           Margin="0,16,0,4"/>
                <ItemsControl ItemsSource="{Binding Examples}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,2,0,4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="24"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" Text="{Binding Command, UpdateSourceTrigger=PropertyChanged}"
                                             AutomationProperties.Name="{loc:Translate ToolCmdLibDialogExampleCommand}"
                                             FontFamily="Consolas" Margin="0,0,4,0" Padding="4,3"
                                             FontSize="{StaticResource FontSizeCaption}"
                                             Background="{DynamicResource CardBrush}"
                                             Foreground="{DynamicResource TextPrimaryBrush}"
                                             BorderBrush="{DynamicResource BorderBrush}"/>
                                    <!-- Value-matched selection (by enum Tag, not index) so the row's
                                         platform displays correctly regardless of item realization order. -->
                                    <ComboBox Grid.Column="1"
                                              SelectedValuePath="Tag"
                                              SelectedValue="{Binding Platform}"
                                              AutomationProperties.Name="{loc:Translate ToolCmdLibDialogLblPlatform}"
                                              Margin="0,0,4,0"
                                              FontSize="{StaticResource FontSizeCaption}">
                                        <ComboBoxItem Tag="{x:Static enums:Platform.Windows}"
                                                      Content="{Binding DataContext.PlatformOptions[0], RelativeSource={RelativeSource AncestorType=Window}}"/>
                                        <ComboBoxItem Tag="{x:Static enums:Platform.Linux}"
                                                      Content="{Binding DataContext.PlatformOptions[1], RelativeSource={RelativeSource AncestorType=Window}}"/>
                                        <ComboBoxItem Tag="{x:Static enums:Platform.Both}"
                                                      Content="{Binding DataContext.PlatformOptions[2], RelativeSource={RelativeSource AncestorType=Window}}"/>
                                    </ComboBox>
                                    <Button Grid.Column="2" Content="&#xE711;" FontFamily="Segoe MDL2 Assets"
                                            FontSize="10" Width="20" Height="20"
                                            AutomationProperties.Name="{loc:Translate ToolCmdLibBtnDelete}"
                                            Command="{Binding DataContext.RemoveExampleCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="{loc:Translate ToolCmdLibBtnDelete}"
                                            Style="{DynamicResource GhostButtonStyle}"/>
                                </Grid>
                                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource WatermarkTextBoxStyle}"
                                         Tag="{loc:Translate ToolCmdLibDialogExampleDescription}"
                                         AutomationProperties.Name="{loc:Translate ToolCmdLibDialogExampleDescription}"
                                         Margin="0,2,4,0" Padding="4,3"
                                         FontSize="{StaticResource FontSizeSmallCaption}"
                                         Background="{DynamicResource CardBrush}"
                                         Foreground="{DynamicResource TextSecondaryBrush}"
                                         BorderBrush="{DynamicResource BorderBrush}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button x:Name="BtnAddExample" Content="{loc:Translate ToolCmdLibDialogBtnAddExample}"
                        Command="{Binding AddExampleCommand}"
                        AutomationProperties.Name="{Binding Content, RelativeSource={RelativeSource Self}}"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        FontSize="{StaticResource FontSizeCaption}"
                        Padding="8,2" HorizontalAlignment="Left" Margin="0,4,0,0"/>

                <!-- ── Documentation links ─────────────────── -->
                <TextBlock Style="{StaticResource DialogSectionTitleStyle}"
                           Text="{loc:Translate ToolCmdLibDialogSectionLinks}"
                           Margin="0,16,0,4"/>
                <ItemsControl ItemsSource="{Binding Links}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="24"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                                         AutomationProperties.Name="{loc:Translate ToolCmdLibDialogLinkTitle}"
                                         Margin="0,0,4,0" Padding="4,3"
                                         FontSize="{StaticResource FontSizeCaption}"
                                         Background="{DynamicResource CardBrush}"
                                         Foreground="{DynamicResource TextPrimaryBrush}"
                                         BorderBrush="{DynamicResource BorderBrush}"/>
                                <TextBox Grid.Column="1" Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}"
                                         AutomationProperties.Name="{loc:Translate ToolCmdLibDialogLinkUrl}"
                                         FontFamily="Consolas" Margin="0,0,4,0" Padding="4,3"
                                         FontSize="{StaticResource FontSizeCaption}"
                                         Background="{DynamicResource CardBrush}"
                                         Foreground="{DynamicResource TextPrimaryBrush}"
                                         BorderBrush="{DynamicResource BorderBrush}"/>
                                <Button Grid.Column="2" Content="&#xE711;" FontFamily="Segoe MDL2 Assets"
                                        FontSize="10" Width="20" Height="20"
                                        AutomationProperties.Name="{loc:Translate ToolCmdLibBtnDelete}"
                                        Command="{Binding DataContext.RemoveLinkCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        ToolTip="{loc:Translate ToolCmdLibBtnDelete}"
                                        Style="{DynamicResource GhostButtonStyle}"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button x:Name="BtnAddLink" Content="{loc:Translate ToolCmdLibDialogBtnAddLink}"
                        Command="{Binding AddLinkCommand}"
                        AutomationProperties.Name="{Binding Content, RelativeSource={RelativeSource Self}}"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        FontSize="{StaticResource FontSizeCaption}"
                        Padding="8,2" HorizontalAlignment="Left" Margin="0,4,0,0"/>
            </StackPanel>
        </ScrollViewer>

        <!-- ═══════════ VALIDATION ERROR ═══════════ -->
        <TextBlock Grid.Row="1" Text="{Binding ValidationError}"
                   Foreground="{DynamicResource ErrorTextBrush}"
                   FontSize="{StaticResource FontSizeBody}"
                   Margin="0,8,0,0" TextWrapping="Wrap"
                   Visibility="{Binding ValidationError, Converter={StaticResource NullToVis}}"/>

        <!-- ═══════════ BUTTONS ═══════════ -->
        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="CancelBtn" IsCancel="True" MinWidth="80"
                    Content="{loc:Translate BtnCancel}"
                    AutomationProperties.Name="{loc:Translate BtnCancel}"
                    Margin="0,0,8,0"
                    Style="{DynamicResource SecondaryButtonStyle}"/>
            <Button x:Name="SaveBtn" IsDefault="True" MinWidth="80"
                    Content="{loc:Translate BtnSave}"
                    AutomationProperties.Name="{loc:Translate BtnSave}"
                    Click="OnSaveClick"
                    Style="{DynamicResource PrimaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
