<UserControl x:Class="Heimdall.App.Views.EmbeddedRdpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:loc="clr-namespace:Heimdall.App.Localization"
             xmlns:rdp="clr-namespace:Heimdall.App.Views.EmbeddedRdp"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
             mc:Ignorable="d"
             d:DesignHeight="540"
             d:DesignWidth="900"
             Background="{DynamicResource BackgroundBrush}">
    <Grid Background="{DynamicResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border x:Name="SessionHeaderBar" Grid.Row="0"
                Padding="{StaticResource SessionHeaderPadding}"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                KeyboardNavigation.TabNavigation="Local"
                KeyboardNavigation.ControlTabNavigation="Local">
            <DockPanel LastChildFill="True">
                <Button x:Name="DisconnectButton"
                        DockPanel.Dock="Right"
                        TabIndex="0"
                        Content="{loc:Translate BtnDisconnectSession}"
                        Click="OnDisconnectClick"
                        ToolTip="{loc:Translate TooltipDisconnectSession}"
                        AutomationProperties.Name="{loc:Translate A11yDisconnectSession}"
                        Visibility="Collapsed"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        Padding="8,2"
                        FontSize="{StaticResource FontSizeCaption}"
                        Margin="8,0,0,0"/>
                <Button x:Name="CancelReconnectButton"
                        DockPanel.Dock="Right"
                        TabIndex="0"
                        Content="{loc:Translate BtnCancelReconnect}"
                        Click="OnCancelReconnectClick"
                        ToolTip="{loc:Translate TooltipCancelReconnect}"
                        AutomationProperties.Name="{loc:Translate A11yCancelReconnect}"
                        Visibility="Collapsed"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        Padding="8,2"
                        FontSize="{StaticResource FontSizeCaption}"
                        Margin="8,0,0,0"/>
                <Button x:Name="CancelConnectButton"
                        DockPanel.Dock="Right"
                        TabIndex="0"
                        Content="{loc:Translate BtnCancelConnect}"
                        Click="OnCancelConnectClick"
                        ToolTip="{loc:Translate TooltipCancelConnect}"
                        AutomationProperties.Name="{loc:Translate A11yCancelConnect}"
                        Visibility="Collapsed"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        Padding="8,2"
                        FontSize="{StaticResource FontSizeCaption}"
                        Margin="8,0,0,0"/>
                <Border AutomationProperties.AutomationId="Toolbar.SeparatorA-B"
                        DockPanel.Dock="Right">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource ToolbarVerticalSeparatorStyle}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Visibility, ElementName=DisconnectButton}" Value="Visible">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Visibility, ElementName=CancelReconnectButton}" Value="Visible">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Visibility, ElementName=CancelConnectButton}" Value="Visible">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                </Border>
                <rdp:AutomationStackPanel x:Name="HealthDot"
                            DockPanel.Dock="Right"
                            Orientation="Horizontal"
                            Margin="8,0,4,0"
                            VerticalAlignment="Center"
                            ToolTip="{loc:Translate TooltipHealthDot}"
                            AutomationProperties.Name="{loc:Translate A11yHealthDot}"
                            AutomationProperties.LiveSetting="Polite"
                            Focusable="False"
                            KeyboardNavigation.IsTabStop="False">
                    <Border x:Name="HealthDotColor"
                            Width="10"
                            Height="10"
                            CornerRadius="5"
                            VerticalAlignment="Center"
                            Background="{DynamicResource TextDisabledBrush}"/>
                    <TextBlock x:Name="HealthDotGlyph"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="{StaticResource FontSizeCaption}"
                               Margin="2,0,0,0"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextDisabledBrush}"/>
                </rdp:AutomationStackPanel>
                <Button x:Name="FullscreenButton"
                        DockPanel.Dock="Right"
                        TabIndex="1"
                        Content="&#xE1D9;"
                        FontFamily="Segoe MDL2 Assets"
                        Click="OnFullscreenButtonClick"
                        ToolTip="{loc:Translate TooltipFullscreenToggle}"
                        AutomationProperties.Name="{loc:Translate A11yFullscreenToggle}"
                        Style="{DynamicResource GhostButtonStyle}"
                        Padding="6,2"
                        FontSize="{StaticResource FontSizeBody}"
                        Margin="4,0"/>
                <Button x:Name="SendKeysButton"
                        DockPanel.Dock="Right"
                        TabIndex="2"
                        Content="&#xE92E;"
                        FontFamily="Segoe MDL2 Assets"
                        Click="OnSendKeysButtonClick"
                        ToolTip="{loc:Translate TooltipSendKeysMenu}"
                        AutomationProperties.Name="{loc:Translate A11ySendKeysMenu}"
                        Style="{DynamicResource GhostButtonStyle}"
                        Padding="6,2"
                        FontSize="{StaticResource FontSizeBody}"
                        Margin="4,0">
                    <Button.ContextMenu>
                        <ContextMenu x:Name="SendKeysMenu" FontFamily="Segoe UI">
                            <MenuItem x:Name="SendKeysCtrlAltDel"
                                      Header="{loc:Translate RdpSendKeysCtrlAltDel}"
                                      Click="OnSendKeysCtrlAltDelClick"/>
                            <Separator/>
                            <MenuItem x:Name="SendKeysWindows"
                                      Header="{loc:Translate RdpSendKeysWindows}"
                                      Click="OnSendKeysWindowsClick"/>
                            <MenuItem x:Name="SendKeysAltTab"
                                      Header="{loc:Translate RdpSendKeysAltTab}"
                                      Click="OnSendKeysAltTabClick"/>
                            <MenuItem x:Name="SendKeysCtrlEsc"
                                      Header="{loc:Translate RdpSendKeysCtrlEsc}"
                                      Click="OnSendKeysCtrlEscClick"/>
                            <Separator/>
                            <MenuItem x:Name="SendKeysPrintScreen"
                                      Header="{loc:Translate RdpSendKeysPrintScreen}"
                                      Click="OnSendKeysPrintScreenClick"/>
                            <MenuItem x:Name="SendKeysEscape"
                                      Header="{loc:Translate RdpSendKeysEscape}"
                                      Click="OnSendKeysEscapeClick"/>
                            <MenuItem x:Name="SendKeysF11"
                                      Header="{loc:Translate RdpSendKeysF11}"
                                      Click="OnSendKeysF11Click"/>
                            <Separator/>
                            <MenuItem x:Name="SendKeysWinL"
                                      Header="{loc:Translate RdpSendKeysWinL}"
                                      Click="OnSendKeysWinLClick"/>
                            <MenuItem x:Name="SendKeysWinD"
                                      Header="{loc:Translate RdpSendKeysWinD}"
                                      Click="OnSendKeysWinDClick"/>
                            <MenuItem x:Name="SendKeysWinE"
                                      Header="{loc:Translate RdpSendKeysWinE}"
                                      Click="OnSendKeysWinEClick"/>
                            <Separator/>
                            <MenuItem x:Name="SendKeysShortcutsHelp"
                                      Header="{loc:Translate RdpShortcutsHelpMenuItem}"
                                      Click="OnSendKeysShortcutsHelpClick"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>
                <Button x:Name="SplitButton"
                        DockPanel.Dock="Right"
                        TabIndex="3"
                        Content="&#xE8A9;"
                        FontFamily="Segoe MDL2 Assets"
                        Click="OnSplitClick"
                        ToolTip="{loc:Translate TooltipSplitSession}"
                        AutomationProperties.Name="{loc:Translate A11ySplitSession}"
                        Style="{DynamicResource GhostButtonStyle}"
                        Padding="6,2" FontSize="{StaticResource FontSizeBody}" Margin="4,0"/>
                <Border Style="{StaticResource ToolbarVerticalSeparatorStyle}"
                        AutomationProperties.AutomationId="Toolbar.SeparatorB-C"
                        DockPanel.Dock="Right"/>
                <Button x:Name="ResolutionButton"
                        DockPanel.Dock="Right"
                        TabIndex="4"
                        Content="&#xE740;"
                        FontFamily="Segoe MDL2 Assets"
                        Style="{DynamicResource GhostButtonStyle}"
                        AutomationProperties.Name="{loc:Translate A11yChangeResolution}"
                        Padding="6,2" FontSize="{StaticResource FontSizeBody}" Margin="4,0"
                        Click="OnResolutionButtonClick">
                    <Button.ContextMenu>
                        <ContextMenu x:Name="ResolutionMenu" FontFamily="Segoe UI">
                            <MenuItem x:Name="ResMenuModeHeader"
                                      IsEnabled="False"
                                      IsHitTestVisible="False"
                                      StaysOpenOnClick="True">
                                <MenuItem.Header>
                                    <TextBlock x:Name="ResMenuModeHeaderText"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </MenuItem.Header>
                            </MenuItem>
                            <Separator x:Name="ResMenuModeHeaderSeparator"/>
                            <MenuItem x:Name="ResMenuSkipStabilization"
                                      Header="{loc:Translate RdpResolutionSkipStabilization}"
                                      Click="OnSkipStabilizationClick"
                                      Visibility="Collapsed"/>
                            <Separator x:Name="ResMenuSkipStabilizationSeparator"
                                       Visibility="Collapsed"/>
                            <MenuItem x:Name="ResMenuFit"
                                      Header="{loc:Translate RdpResolutionFitToWindow}"
                                      Click="OnResolutionMenuClick"
                                      Tag="Fit"/>
                            <Separator/>
                            <!-- Preset items populated dynamically by PopulateResolutionMenu()
                                 from AppSettings.RdpResolutionPresets, with a built-in fallback.
                                 Items 0-5 are static (mode header + separator + skip-stab + skip-stab-sep
                                 + fit + separator); presets are appended at index 6+. -->
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="SessionTitleText"
                               FontSize="{StaticResource FontSizeBody}"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="EndpointTextBlock"
                               Margin="8,0,0,0"
                               FontSize="{StaticResource FontSizeCaption}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>
                    <TextBlock Text=" - " Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="{StaticResource FontSizeCaption}" VerticalAlignment="Center"/>
                    <!-- No AutomationProperties.Name: a name wins over the element's own text,
                         so a constant one announces "Connection status" in place of whichever of
                         the thirty-odd status messages is showing. The role wording lives in
                         HelpText, and the five sibling status lines below already do this. -->
                    <TextBlock x:Name="StatusTextBlock"
                               FontSize="{StaticResource FontSizeCaption}"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               VerticalAlignment="Center"
                               AutomationProperties.HelpText="{loc:Translate A11yConnectionStatus}"
                               AutomationProperties.LiveSetting="Polite"/>
                    <TextBlock x:Name="AutofillSeparator"
                               Text=" &#x2022; "
                               Margin="6,0,0,0"
                               FontSize="{StaticResource FontSizeCaption}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Visibility="Collapsed"/>
                    <TextBlock x:Name="AutofillStatusText"
                               FontSize="{StaticResource FontSizeCaption}"
                               FontStyle="Italic"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Visibility="Collapsed"
                               AutomationProperties.LiveSetting="Polite"/>
                    <Button x:Name="AutofillRetryButton"
                            Content="{loc:Translate RdpAutofillRetryAction}"
                            Click="OnAutofillRetryClick"
                            ToolTip="{loc:Translate RdpAutofillRetryTooltip}"
                            AutomationProperties.Name="{loc:Translate RdpAutofillRetryAction}"
                            Style="{DynamicResource GhostButtonStyle}"
                            Margin="6,0,0,0"
                            Padding="8,2"
                            FontSize="{StaticResource FontSizeCaption}"
                            Visibility="Collapsed"/>
                    <Button x:Name="AutofillDismissButton"
                            Content="&#xE711;"
                            FontFamily="Segoe MDL2 Assets"
                            Click="OnAutofillDismissClick"
                            ToolTip="{loc:Translate RdpAutofillDismissTooltip}"
                            AutomationProperties.Name="{loc:Translate RdpAutofillDismissAction}"
                            Style="{DynamicResource GhostButtonStyle}"
                            Margin="2,0,0,0"
                            Padding="6,2"
                            FontSize="{StaticResource FontSizeCaption}"
                            Visibility="Collapsed"/>
                    <TextBlock x:Name="StabilizingSeparator"
                               Text=" &#x2022; "
                               Margin="6,0,0,0"
                               FontSize="{StaticResource FontSizeCaption}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Visibility="Collapsed"/>
                    <!-- No AutomationProperties.LiveSetting on the three timers below: they are rewritten
                         once a second, and a polite live region firing every second is a flood
                         rather than an announcement. The status line beside them carries the state. -->
                    <TextBlock x:Name="StabilizingStatusText"
                               FontSize="{StaticResource FontSizeCaption}"
                               FontStyle="Italic"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Visibility="Collapsed"/>
                    <TextBlock x:Name="NextRetrySeparator"
                               Text=" &#x2022; "
                               Margin="6,0,0,0"
                               FontSize="{StaticResource FontSizeCaption}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Visibility="Collapsed"/>
                    <TextBlock x:Name="NextRetryText"
                               FontSize="{StaticResource FontSizeCaption}"
                               FontStyle="Italic"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Visibility="Collapsed"/>
                    <TextBlock x:Name="ReconnectElapsedSeparator"
                               Text=" &#x2022; "
                               Margin="6,0,0,0"
                               FontSize="{StaticResource FontSizeCaption}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Visibility="Collapsed"/>
                    <TextBlock x:Name="ReconnectElapsedText"
                               FontSize="{StaticResource FontSizeCaption}"
                               FontStyle="Italic"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Visibility="Collapsed"/>
                    <rdp:AutomationStackPanel x:Name="ConnectionPhaseStepper"
                                Orientation="Horizontal"
                                Margin="12,0,0,0"
                                VerticalAlignment="Center"
                                Focusable="False"
                                KeyboardNavigation.IsTabStop="False"
                                Visibility="Collapsed"
                                AutomationProperties.Name="{loc:Translate A11yConnectionPhaseStepper}"
                                AutomationProperties.LiveSetting="Polite">
                        <Border x:Name="PhaseSegmentPreparing"
                                Width="14"
                                Height="3"
                                Margin="0,0,3,0"
                                CornerRadius="1.5"
                                Background="{DynamicResource TextDisabledBrush}"/>
                        <Border x:Name="PhaseSegmentConnecting"
                                Width="14"
                                Height="3"
                                Margin="0,0,3,0"
                                CornerRadius="1.5"
                                Background="{DynamicResource TextDisabledBrush}"/>
                        <Border x:Name="PhaseSegmentLoading"
                                Width="14"
                                Height="3"
                                Margin="0,0,3,0"
                                CornerRadius="1.5"
                                Background="{DynamicResource TextDisabledBrush}"/>
                        <Border x:Name="PhaseSegmentConnected"
                                Width="14"
                                Height="3"
                                CornerRadius="1.5"
                                Background="{DynamicResource TextDisabledBrush}"/>
                    </rdp:AutomationStackPanel>
                    <Button x:Name="AntiIdleBadge"
                            TabIndex="5"
                            Click="OnAntiIdleBadgeClick"
                            ToolTip="{loc:Translate TooltipAntiIdleActive}"
                            AutomationProperties.Name="{loc:Translate A11yAntiIdleActive}"
                            Visibility="Collapsed"
                            Margin="8,0,0,0"
                            Padding="4,1"
                            VerticalAlignment="Center"
                            Style="{DynamicResource GhostButtonStyle}"
                            Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE945;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="{StaticResource FontSizeCaption}"
                                       Foreground="{DynamicResource AccentBrush}"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <rdp:AutomationBorder x:Name="RedirectionIndicatorsPanel"
                            Margin="12,0,0,0"
                            Padding="6,1"
                            CornerRadius="4"
                            Background="{DynamicResource CardBrush}"
                            Focusable="False"
                            KeyboardNavigation.IsTabStop="False"
                            Visibility="Collapsed"
                            VerticalAlignment="Center"
                            AutomationProperties.LiveSetting="Polite">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="RedirIconClipboard"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="{StaticResource FontSizeCaption}"
                                       Margin="0,0,6,0"
                                       VerticalAlignment="Center"
                                       AutomationProperties.Name="{loc:Translate RdpRedirectionLabelClipboard}"
                                       AutomationProperties.HelpText="{loc:Translate RdpRedirectionLabelClipboard}"/>
                            <TextBlock x:Name="RedirIconDrives"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="{StaticResource FontSizeCaption}"
                                       Margin="0,0,6,0"
                                       VerticalAlignment="Center"
                                       AutomationProperties.Name="{loc:Translate RdpRedirectionLabelDrives}"
                                       AutomationProperties.HelpText="{loc:Translate RdpRedirectionLabelDrives}"/>
                            <TextBlock x:Name="RedirIconPrinters"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="{StaticResource FontSizeCaption}"
                                       Margin="0,0,6,0"
                                       VerticalAlignment="Center"
                                       AutomationProperties.Name="{loc:Translate RdpRedirectionLabelPrinters}"
                                       AutomationProperties.HelpText="{loc:Translate RdpRedirectionLabelPrinters}"/>
                            <TextBlock x:Name="RedirIconComPorts"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="{StaticResource FontSizeCaption}"
                                       Margin="0,0,6,0"
                                       VerticalAlignment="Center"
                                       AutomationProperties.Name="{loc:Translate RdpRedirectionLabelComPorts}"
                                       AutomationProperties.HelpText="{loc:Translate RdpRedirectionLabelComPorts}"/>
                            <TextBlock x:Name="RedirIconSmartCards"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="{StaticResource FontSizeCaption}"
                                       Margin="0,0,6,0"
                                       VerticalAlignment="Center"
                                       AutomationProperties.Name="{loc:Translate RdpRedirectionLabelSmartCards}"
                                       AutomationProperties.HelpText="{loc:Translate RdpRedirectionLabelSmartCards}"/>
                            <TextBlock x:Name="RedirIconUsb"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="{StaticResource FontSizeCaption}"
                                       Margin="0,0,6,0"
                                       VerticalAlignment="Center"
                                       AutomationProperties.Name="{loc:Translate RdpRedirectionLabelUsb}"
                                       AutomationProperties.HelpText="{loc:Translate RdpRedirectionLabelUsb}"/>
                            <TextBlock x:Name="RedirIconAudio"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="{StaticResource FontSizeCaption}"
                                       Margin="0,0,6,0"
                                       VerticalAlignment="Center"
                                       AutomationProperties.Name="{loc:Translate RdpRedirectionLabelAudio}"
                                       AutomationProperties.HelpText="{loc:Translate RdpRedirectionLabelAudio}"/>
                            <TextBlock x:Name="RedirIconMultiMonitor"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="{StaticResource FontSizeCaption}"
                                       VerticalAlignment="Center"
                                       AutomationProperties.Name="{loc:Translate RdpRedirectionLabelMultiMonitor}"
                                       AutomationProperties.HelpText="{loc:Translate RdpRedirectionLabelMultiMonitor}"/>
                            <Button x:Name="RedirExpandBadge"
                                    Visibility="Collapsed"
                                    Margin="6,0,0,0"
                                    Padding="4,0"
                                    FontSize="{StaticResource FontSizeCaption}"
                                    Foreground="{DynamicResource TextSecondaryBrush}"
                                    Style="{DynamicResource GhostButtonStyle}"
                                    Click="OnRedirExpandBadgeClick"
                                    Cursor="Hand"
                                    ToolTip="{loc:Translate RdpRedirectionExpandBadgeTooltip}"
                                    AutomationProperties.Name="{loc:Translate RdpRedirectionExpandBadgeA11y}"/>
                        </StackPanel>
                    </rdp:AutomationBorder>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1">
            <Border Margin="16,0,16,16"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource CornerRadiusXl}">
                <Border Background="{DynamicResource SurfaceBrush}"
                        ClipToBounds="True">
                    <Grid x:Name="SurfaceContainer"
                          Background="{DynamicResource SurfaceBrush}"
                          ClipToBounds="True">
                        <Border x:Name="RdpRegionFrame"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                Padding="0"
                                Margin="0"
                                SnapsToDevicePixels="True"
                                UseLayoutRounding="True"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch">
                            <wfi:WindowsFormsHost x:Name="FormsHost"
                                                  HorizontalAlignment="Stretch"
                                                  VerticalAlignment="Stretch"
                                                  Background="{DynamicResource SurfaceBrush}"
                                                  Margin="0"
                                                  KeyboardNavigation.IsTabStop="False"
                                                  KeyboardNavigation.TabNavigation="None"/>
                        </Border>

                        <Border x:Name="LetterboxHintBadge"
                                Background="{DynamicResource BorderBrush}"
                                CornerRadius="{StaticResource CornerRadiusSm}"
                                Padding="6,3"
                                Margin="0,0,12,12"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                Visibility="Collapsed"
                                Opacity="0"
                                IsHitTestVisible="False"
                                Focusable="False"
                                SnapsToDevicePixels="True"
                                UseLayoutRounding="True">
                            <TextBlock x:Name="LetterboxHintText"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="{StaticResource FontSizeCaption}"
                                       TextWrapping="NoWrap"
                                       Focusable="False"
                                       IsHitTestVisible="False"/>
                        </Border>
                    </Grid>
                </Border>
            </Border>

            <!-- Reconnect overlay (shown on unexpected disconnect) -->
            <rdp:AutomationBorder x:Name="ReconnectOverlay"
                    Style="{StaticResource FadeInPanelStyle}"
                    Visibility="Collapsed"
                    Focusable="True"
                    KeyboardNavigation.IsTabStop="False"
                    KeyboardNavigation.TabNavigation="Cycle"
                    KeyboardNavigation.ControlTabNavigation="Cycle"
                    Background="{DynamicResource OverlayBackground}"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    AutomationProperties.IsDialog="True"
                    AutomationProperties.Name="{loc:Translate A11yRdpReconnectDialog}"
                    PreviewKeyDown="OnReconnectOverlayPreviewKeyDown">
                <StackPanel VerticalAlignment="Center"
                            HorizontalAlignment="Center">
                    <!-- Message text group: capped width so long messages wrap nicely. -->
                    <StackPanel MaxWidth="460">
                    <Border x:Name="OverlaySeverityStrip"
                            Height="4"
                            Margin="0,0,0,16"
                            HorizontalAlignment="Stretch"
                            Background="{DynamicResource ErrorBrush}"
                            CornerRadius="2"/>
                    <TextBlock x:Name="OverlaySeverityIcon"
                               Margin="0,0,0,12"
                               HorizontalAlignment="Center"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="32"
                               Text="&#xE783;"
                               Foreground="{DynamicResource ErrorTextBrush}"
                               Focusable="False"
                               IsHitTestVisible="False"
                               AutomationProperties.Name=" "/>
                    <TextBlock x:Name="ReconnectMessageText"
                               MaxWidth="400"
                               HorizontalAlignment="Center"
                               FontSize="{StaticResource FontSizeSubtitle}"
                               FontWeight="SemiBold"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               AutomationProperties.LiveSetting="Polite" />
                    <TextBlock x:Name="ReconnectSecondaryText"
                               Margin="0,8,0,0"
                               MaxWidth="400"
                               HorizontalAlignment="Center"
                               FontSize="{StaticResource FontSizeBody}"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Visibility="Collapsed" />
                    <TextBlock x:Name="ReconnectCodeText"
                               Margin="0,6,0,0"
                               FontSize="{StaticResource FontSizeCaption}"
                               TextAlignment="Center"
                               Foreground="{DynamicResource TextDisabledBrush}"
                               Visibility="Collapsed" />
                    </StackPanel>
                    <!-- Button row: WrapPanel so localized labels wrap to a second line instead of clipping. -->
                    <WrapPanel Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                MaxWidth="720"
                                Margin="0,16,0,0"
                                KeyboardNavigation.TabNavigation="Cycle">
                        <Button x:Name="OverlayReconnectButton"
                                TabIndex="0"
                                Click="OnOverlayReconnectClick"
                                Content="{loc:Translate BtnReconnectSession}"
                                AutomationProperties.Name="{loc:Translate A11yReconnectSession}"
                                Style="{DynamicResource PrimaryButtonStyle}"
                                Padding="16,6"
                                FontSize="{StaticResource FontSizeBodyLarge}"
                                Margin="0,0,8,0"/>
                        <Button x:Name="OverlayCopyErrorButton"
                                TabIndex="1"
                                Click="OnOverlayCopyErrorClick"
                                Content="{loc:Translate BtnCopyErrorOverlay}"
                                ToolTip="{loc:Translate TooltipCopyErrorOverlay}"
                                AutomationProperties.Name="{loc:Translate A11yCopyErrorOverlay}"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Padding="16,6"
                                FontSize="{StaticResource FontSizeBodyLarge}"
                                Margin="8,0,0,0"/>
                        <Button x:Name="OverlayEditProfileButton"
                                TabIndex="2"
                                Click="OnOverlayEditProfileClick"
                                Content="{loc:Translate BtnEditProfileOverlay}"
                                ToolTip="{loc:Translate TooltipEditProfileOverlay}"
                                AutomationProperties.Name="{loc:Translate A11yEditProfileOverlay}"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Padding="16,6"
                                FontSize="{StaticResource FontSizeBodyLarge}"
                                Margin="8,0,0,0"
                                Visibility="Collapsed"/>
                        <Button x:Name="OverlayCloseButton"
                                TabIndex="3"
                                Click="OnOverlayCloseClick"
                                Content="{loc:Translate BtnCloseOverlay}"
                                AutomationProperties.Name="{loc:Translate A11yCloseOverlay}"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Padding="16,6"
                                FontSize="{StaticResource FontSizeBodyLarge}"
                                Margin="8,0,0,0"/>
                    </WrapPanel>
                </StackPanel>
            </rdp:AutomationBorder>

            <!--
                Certificate question. Application-NONMODAL and session-blocking: it stops this
                connection and nothing else, which is why it lives here rather than in a
                top-level Window. AutomationBorder rather than Border because a plain Border has
                no automation peer, so IsDialog and Name on one are read by nothing.
            -->
            <rdp:AutomationBorder x:Name="CertificatePromptOverlay"
                    Visibility="Collapsed"
                    Focusable="True"
                    Panel.ZIndex="60"
                    KeyboardNavigation.IsTabStop="False"
                    KeyboardNavigation.TabNavigation="Cycle"
                    KeyboardNavigation.ControlTabNavigation="Cycle"
                    Background="{DynamicResource OverlayBackground}"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    AutomationProperties.IsDialog="True"
                    AutomationProperties.Name="{loc:Translate A11yRdpCertificatePrompt}"
                    PreviewKeyDown="OnCertificatePromptPreviewKeyDown">
                <Border MaxWidth="640"
                        Margin="24"
                        Padding="20"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource CornerRadiusXl}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Margin="0,0,10,0"
                                       VerticalAlignment="Top"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="24"
                                       Text="&#xE783;"
                                       Foreground="{DynamicResource WarningBrush}"
                                       Focusable="False"
                                       IsHitTestVisible="False"
                                       AutomationProperties.Name=" "/>
                            <TextBlock x:Name="CertificatePromptTitleText"
                                       MaxWidth="540"
                                       FontSize="{StaticResource FontSizeSubtitle}"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       Text="{Binding Title}"/>
                        </StackPanel>

                        <!--
                            No AutomationProperties.Name here, on this or on any other text
                            below: a static name masks a TextBlock's live text, so a screen
                            reader would announce the label instead of the question.
                        -->
                        <TextBlock x:Name="CertificatePromptMessageText"
                                   MaxWidth="580"
                                   Margin="0,0,0,12"
                                   TextWrapping="Wrap"
                                   FontSize="{StaticResource FontSizeBody}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   AutomationProperties.LiveSetting="Assertive"
                                   Text="{Binding Message}"/>

                        <TextBlock x:Name="CertificatePromptHostLabel"
                                   FontSize="{StaticResource FontSizeCaption}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   Text="{Binding RemoteEndpointLabel}"/>
                        <TextBlock x:Name="CertificatePromptHostText"
                                   Margin="0,2,0,10"
                                   TextWrapping="Wrap"
                                   FontSize="{StaticResource FontSizeBody}"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   Text="{Binding RemoteEndpoint}"/>

                        <!--
                            The gateway chain, which is the half of the machine's identity the
                            endpoint above cannot carry. Two profiles reaching one short name
                            through two different gateways are two machines, and their endpoint
                            text differs only by an ephemeral local tunnel port. No static
                            AutomationProperties.Name on either block: one would mask the live
                            text a screen reader has to hear.
                        -->
                        <TextBlock x:Name="CertificatePromptRouteLabel"
                                   FontSize="{StaticResource FontSizeCaption}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   Text="{Binding RouteLabel}"
                                   Visibility="{Binding HasRoute, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <TextBlock x:Name="CertificatePromptRouteText"
                                   Margin="0,2,0,10"
                                   TextWrapping="Wrap"
                                   FontSize="{StaticResource FontSizeBody}"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   Text="{Binding Route}"
                                   Visibility="{Binding HasRoute, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <TextBlock x:Name="CertificatePromptThumbprintLabel"
                                   FontSize="{StaticResource FontSizeCaption}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   Text="{Binding ThumbprintLabel}"/>
                        <TextBox x:Name="CertificatePromptThumbprintBox"
                                 Margin="0,2,0,10"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 FontFamily="{StaticResource FontFamilyMonospace}"
                                 FontSize="{StaticResource FontSizeBody}"
                                 Background="{DynamicResource BackgroundBrush}"
                                 BorderBrush="{DynamicResource BorderBrush}"
                                 BorderThickness="1"
                                 Padding="8"
                                 Text="{Binding Thumbprint, Mode=OneWay}"/>

                        <TextBlock x:Name="CertificatePromptSubjectText"
                                   Margin="0,0,0,10"
                                   TextWrapping="Wrap"
                                   FontSize="{StaticResource FontSizeCaption}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   Text="{Binding Subject}"
                                   Visibility="{Binding HasSubject, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <TextBlock x:Name="CertificatePromptAlreadyTrustedText"
                                   Margin="0,0,0,10"
                                   TextWrapping="Wrap"
                                   FontSize="{StaticResource FontSizeBody}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   Text="{Binding AlreadyTrustedText}"
                                   Visibility="{Binding HasAlreadyTrustedText, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <TextBlock x:Name="CertificatePromptOwnerText"
                                   Margin="0,0,0,4"
                                   TextWrapping="Wrap"
                                   FontSize="{StaticResource FontSizeCaption}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   Text="{Binding OwnerText}"
                                   Visibility="{Binding HasOwnerText, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <!--
                            Refusal first and focused: the answer an accidental keystroke gives
                            is the one that creates no durable trust.

                            NO KeyboardNavigation.TabNavigation HERE, deliberately, and unlike
                            the reconnect overlay above. A nested Cycle scope with focus placed
                            inside it is a trap within a trap: Tab returns to the first button
                            instead of leaving, so the three answers are all a keyboard user can
                            ever reach. The fingerprint they are being asked to compare sits in a
                            read-only TextBox that is a tab stop and was unreachable - never
                            focusable, never selectable, never copyable, and never read out by a
                            screen reader walking the question with Tab. The overlay's own Cycle
                            scope is the single trap, so Tab now reaches that box before coming
                            back round to the buttons.

                            The box is the whole gain, and the only one. Every other line of the
                            question - the endpoint, the route, the owner line - is a TextBlock,
                            which WPF gives Focusable=False, so none of them was ever a tab stop
                            and none of them becomes one here. A screen reader reads them from
                            the dialog's content; making them focusable to satisfy a sentence
                            would lengthen the tab cycle of a security question for nothing.

                            KeyboardNavigation.AcceptsReturn on each answer, and it is not
                            decoration: it is the only thing that makes Enter answer at all.
                            ButtonBase.OnKeyDown reads it and clicks the focused button, which is
                            a real OnClick - the Click event AND the command source. These three
                            answers are bound commands, so the code-behind's old habit of raising
                            ButtonBase.ClickEvent by hand ran nothing: Enter on "Do not connect"
                            recorded no refusal and the pane adopted another pane's approval.
                        -->
                        <WrapPanel x:Name="CertificatePromptAnswers"
                                   Orientation="Horizontal"
                                   HorizontalAlignment="Right"
                                   Margin="0,12,0,0">
                            <Button x:Name="CertificatePromptRefuseButton"
                                    TabIndex="0"
                                    KeyboardNavigation.AcceptsReturn="True"
                                    MinWidth="140"
                                    Margin="0,0,8,0"
                                    Padding="14,6"
                                    Content="{Binding RefuseButtonText}"
                                    Command="{Binding RefuseCommand}"/>
                            <Button x:Name="CertificatePromptTrustOnceButton"
                                    TabIndex="1"
                                    KeyboardNavigation.AcceptsReturn="True"
                                    MinWidth="150"
                                    MaxWidth="240"
                                    Margin="0,0,8,0"
                                    Padding="14,6"
                                    Command="{Binding TrustOnceCommand}"
                                    Style="{DynamicResource SecondaryButtonStyle}">
                                <TextBlock Text="{Binding TrustOnceButtonText}"
                                           TextAlignment="Center"
                                           TextWrapping="Wrap"/>
                            </Button>
                            <Button x:Name="CertificatePromptTrustButton"
                                    TabIndex="2"
                                    KeyboardNavigation.AcceptsReturn="True"
                                    MinWidth="200"
                                    MaxWidth="300"
                                    Padding="14,6"
                                    Command="{Binding TrustCommand}"
                                    Style="{DynamicResource PrimaryButtonStyle}">
                                <TextBlock Text="{Binding TrustButtonText}"
                                           TextAlignment="Center"
                                           TextWrapping="Wrap"/>
                            </Button>
                        </WrapPanel>
                    </StackPanel>
                </Border>
            </rdp:AutomationBorder>

            <!-- Connection progress indicator -->
            <ProgressBar x:Name="RdpLoadingBar"
                         VerticalAlignment="Top"
                         Height="2"
                         Margin="16,0,16,0"
                         IsIndeterminate="True"
                         Foreground="{DynamicResource AccentBrush}"
                         Background="Transparent"
                         BorderThickness="0"
                         IsHitTestVisible="False"/>

            <rdp:AutomationBorder x:Name="TransientToast"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Margin="0,16,0,0"
                    CornerRadius="{StaticResource CornerRadiusMd}"
                    Padding="12,6"
                    Background="{DynamicResource OverlayBackground}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    Visibility="Collapsed"
                    IsHitTestVisible="False"
                    Focusable="False"
                    KeyboardNavigation.IsTabStop="False"
                    Panel.ZIndex="50"
                    AutomationProperties.LiveSetting="Polite"
                    AutomationProperties.Name="{loc:Translate A11yRdpTransientToast}">
                <TextBlock x:Name="TransientToastText"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           FontSize="{StaticResource FontSizeBody}"/>
            </rdp:AutomationBorder>
        </Grid>
    </Grid>
</UserControl>
